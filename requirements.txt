# --- Core Network & Configuration ---
websockets>=12.0
python-dotenv>=1.0

# --- Audio Input/Output ---
numpy>=1.24
pyaudio>=0.2.14
# Thư viện hỗ trợ bắt âm thanh hệ thống (Loopback) trên Windows
pyaudiowpatch>=0.2.12; sys_platform == 'win32'
# Thư viện âm thanh dự phòng
soundcard>=0.4.2

# --- AI Engine (Inference) ---
# Faster-Whisper dùng CTranslate2 bên dưới để chạy ASR cực nhanh
faster-whisper>=1.0.0

# CTranslate2: Engine chạy model dịch (MarianMT) và ASR
ctranslate2>=4.0.0

# --- Model Utilities (Conversion & Tokenization) ---
# Torch cần thiết để convert model HuggingFace sang CTranslate2 lần đầu
torch>=2.0.0
transformers>=4.36.0
sentencepiece>=0.1.99
sacremoses>=0.0.53

# --- Voice Activity Detection (VAD) ---
onnxruntime>=1.16.0

# --- GPU Acceleration (NVIDIA) ---
# Quan trọng: CTranslate2 yêu cầu các thư viện này để chạy GPU trên Windows/Linux
# mà không cần cài CUDA Toolkit thủ công vào hệ điều hành.
nvidia-cublas-cu12>=12.1.3.1; sys_platform != 'darwin'
nvidia-cudnn-cu12>=9.1.0.70; sys_platform != 'darwin'
nvidia-cuda-runtime-cu12>=12.1.105; sys_platform != 'darwin'
