# --- Core Network & Configuration ---
websockets>=12.0
python-dotenv>=1.0

# --- Audio Input/Output ---
numpy>=1.24
pyaudio>=0.2.14
pyaudiowpatch>=0.2.12; sys_platform == 'win32'
soundcard>=0.4.2

# --- AI Engine (Inference) ---
faster-whisper>=1.0.0

ctranslate2>=4.0.0

# --- Model Utilities (Conversion & Tokenization) ---
torch>=2.0.0
transformers>=4.36.0
sentencepiece>=0.1.99
sacremoses>=0.0.53

# --- Voice Activity Detection (VAD) ---
onnxruntime>=1.16.0

# --- GPU Acceleration (NVIDIA) ---
nvidia-cublas-cu12>=12.1.3.1; sys_platform != 'darwin'
nvidia-cudnn-cu12>=9.1.0.70; sys_platform != 'darwin'
nvidia-cuda-runtime-cu12>=12.1.105; sys_platform != 'darwin'
